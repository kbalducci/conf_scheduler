<h1>Conference room show page</h1>

  <%= @room.room_name %>
  <%= @room.location %>
  <%= @room.description %>
  <%= image_tag @room.photo.url(:medium) %>

  <% @room.meetings.each do |meeting| %>
    <%= meeting.title %>
    <%= meeting.start_time %>
    <%= meeting.end_time %>
  <% end %>

