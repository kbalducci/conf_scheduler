<%= simple_form_for([@meeting.room, @meeting, @comment]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :message %>

  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

